# 사용자별 git 설정
- git config --global user.name "사용자 이름"
- git config --global user.email "사용자 이메일"

# 사용자 폴더에 git clone
- git clone https://github.com/re-trust/insu_platform_web.git platform-chill

# yarn install 후 script 사용
- yarn build:claim 사용


