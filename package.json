{
  "name": "asd",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "@emotion/react": "^11.11.4",
    "@emotion/styled": "^11.11.5",
    "@fortawesome/fontawesome-svg-core": "^6.5.1",
    "@fortawesome/free-solid-svg-icons": "^6.5.1",
    "@fortawesome/react-fontawesome": "^0.2.0",
    "@iconify/react": "^5.0.1",
    "@material-ui/core": "^4.12.4",
    "@mui/icons-material": "^5.15.19",
    "@mui/lab": "^5.0.0-alpha.170",
    "@mui/material": "^5.15.19",
    "@mui/x-date-pickers": "^7.2.0",
    "@react-pdf/renderer": "^4.0.0",
    "@reduxjs/toolkit": "^2.2.3",
    "@tanstack/react-query": "^5.62.0",
    "@testing-library/jest-dom": "^5.14.1",
    "@testing-library/react": "^13.0.0",
    "@testing-library/user-event": "^13.2.1",
    "@types/pdfmake": "^0.2.9",
    "autoprefixer": "^10.4.19",
    "axios": "^1.7.2",
    "crypto-browserify": "^3.12.0",
    "crypto-js": "^4.2.0",
    "date-fns": "^3.6.0",
    "dayjs": "^1.11.10",
    "dotenv": "^16.4.5",
    "init": "^0.1.2",
    "jsencrypt": "^3.3.2",
    "jspdf": "^2.5.2",
    "jspdf-autotable": "^3.8.4",
    "luxon": "^3.4.4",
    "moment": "^2.30.1",
    "node-forge": "^1.3.1",
    "postcss": "^8.4.38",
    "qrcode.react": "^4.1.0",
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "react-gtm-module": "^2.0.11",
    "react-icons": "^5.3.0",
    "react-pdf": "^9.2.1",
    "react-redux": "^9.1.0",
    "react-router-dom": "^6.22.3",
    "react-scripts": "5.0.1",
    "react-slick": "^0.30.2",
    "react-spring": "^9.7.3",
    "reselect": "^5.1.0",
    "session": "^0.1.0",
    "slick-carousel": "^1.8.1",
    "store": "^2.0.12",
    "swiper": "^11.1.4",
    "tailwind-scrollbar-hide": "^1.1.7",
    "tailwindcss": "^3.4.3",
    "uikit": "^3.21.5",
    "web-vitals": "^2.1.0",
    "xlsx": "^0.18.5"
  },
  "scripts": {
    "start": "NODE_ENV=development react-scripts start",
    "build": "NODE_ENV=production GENERATE_SOURCEMAP=false react-scripts build",
    "dev": "NODE_ENV=production react-scripts build",
    "test": "react-scripts test",
    "eject": "react-scripts eject",
    "start:claim": "cross-env NODE_ENV=development REACT_APP_TYPE=CLAIM PUBLIC_URL=/claim craco start",
    "start:overseas": "cross-env NODE_ENV=development REACT_APP_TYPE=OVERSEAS PUBLIC_URL=/trip/overseas craco start",
    "start:departed": "cross-env NODE_ENV=development REACT_APP_TYPE=DEPARTED PUBLIC_URL=/trip/departed craco start",
    "start:trip": "cross-env NODE_ENV=development REACT_APP_TYPE=TRIP PUBLIC_URL=/trip  craco start",
    "start:partner": "cross-env NODE_ENV=development REACT_APP_TYPE=PARTNER PUBLIC_URL=/  craco start",
    "start:safety": "cross-env NODE_ENV=development REACT_APP_TYPE=SAFETY PUBLIC_URL=/safety  craco start",
    "start:disaster": "cross-env NODE_ENV=development REACT_APP_TYPE=DISASTER REACT_APP_DISASTER_APP=safeguard PUBLIC_URL=/safety/disaster  craco start",
    "start:safeguard": "cross-env NODE_ENV=development REACT_APP_TYPE=DISASTER REACT_APP_DISASTER_APP=safeguard PUBLIC_URL=/safety/disasterSafeguard  craco start",
    "start:house": "cross-env NODE_ENV=development REACT_APP_TYPE=DISASTER REACT_APP_DISASTER_APP=house PUBLIC_URL=/safety/disasterHouse  craco start",
    "start:governance": "cross-env NODE_ENV=development REACT_APP_TYPE=DISASTER REACT_APP_DISASTER_APP=governance PUBLIC_URL=/safety/disasterGovernance  craco start",
    "start:fire": "cross-env NODE_ENV=development REACT_APP_TYPE=FIRE PUBLIC_URL=/safety/fire  craco start",
    "start:longterm": "cross-env NODE_ENV=development REACT_APP_TYPE=LONGTERM PUBLIC_URL=/trip/longterm  craco start",
    "start:domestic": "cross-env NODE_ENV=development REACT_APP_TYPE=DOMESTIC PUBLIC_URL=/trip/domestic craco start",
    "start:home": "cross-env NODE_ENV=development REACT_APP_TYPE=HOME PUBLIC_URL=/  craco start",
    "start:certificate": "cross-env NODE_ENV=development REACT_APP_TYPE=CERTIFICATE PUBLIC_URL=/certificate  craco start",
    "build:claim": "rm -rf build && cross-env NODE_OPTIONS=--max_old_space_size=4096 NODE_ENV=production GENERATE_SOURCEMAP=false REACT_APP_TYPE=CLAIM PUBLIC_URL=/claim craco build && if [ -d 'build/static' ]; then rm -rf /var/www/claim && mkdir -p /var/www/claim && cp -r build/* /var/www/claim/; else echo 'Build failed, not copying files'; exit 1; fi",
    "build:overseas": "rm -rf build && cross-env NODE_OPTIONS=--max_old_space_size=4096 NODE_ENV=production GENERATE_SOURCEMAP=false REACT_APP_TYPE=OVERSEAS REACT_APP_PRODUCT_CD=15540 PUBLIC_URL=/trip/overseas craco build && if [ -d 'build/static' ]; then rm -rf /var/www/trip/overseas && mkdir -p /var/www/trip/overseas && cp -r build/* /var/www/trip/overseas/; else echo 'Build failed, not copying files'; exit 1; fi",
    "build:departed": "sudo rm -rf build && cross-env NODE_OPTIONS=--max_old_space_size=4096 NODE_ENV=production GENERATE_SOURCEMAP=false REACT_APP_TYPE=DEPARTED REACT_APP_PRODUCT_CD=15540 PUBLIC_URL=/trip/departed craco build && if [ -d 'build/static' ]; then sudo rm -rf /var/www/trip/departed && sudo mkdir -p /var/www/trip/departed && sudo cp -r build/* /var/www/trip/departed/; else echo 'Build failed, not copying files'; exit 1; fi",
    "build:trip": "echo '🚀 Starting build for trip and all subpages...' && rm -rf build && cross-env NODE_OPTIONS=--max_old_space_size=4096 NODE_ENV=production GENERATE_SOURCEMAP=false REACT_APP_TYPE=TRIP PUBLIC_URL=/trip craco build && if [ -d 'build/static' ]; then rm -rf /var/www/trip && mkdir -p /var/www/trip && cp -r build/* /var/www/trip/; else echo 'Build failed, not copying files'; exit 1; fi && echo '✅ Trip main completed' && npm run build:overseas && echo '✅ Overseas completed' && npm run build:departed && echo '✅ Departed completed' && npm run build:longterm && echo '✅ Longterm completed' && npm run build:domestic && echo '✅ Domestic completed' && echo '🎉 All trip builds completed successfully!'",
    "build:longterm": "rm -rf build && cross-env NODE_OPTIONS=--max_old_space_size=4096 NODE_ENV=production GENERATE_SOURCEMAP=false REACT_APP_TYPE=LONGTERM REACT_APP_PRODUCT_CD=15670 PUBLIC_URL=/trip/longterm craco build && if [ -d 'build/static' ]; then rm -rf /var/www/trip/longterm && mkdir -p /var/www/trip/longterm && cp -r build/* /var/www/trip/longterm/; else echo 'Build failed, not copying files'; exit 1; fi",
    "build:domestic": "rm -rf build && cross-env NODE_OPTIONS=--max_old_space_size=4096 NODE_ENV=production GENERATE_SOURCEMAP=false REACT_APP_TYPE=DOMESTIC REACT_APP_PRODUCT_CD=15920 PUBLIC_URL=/trip/domestic craco build && if [ -d 'build/static' ]; then rm -rf /var/www/trip/domestic && mkdir -p /var/www/trip/domestic && cp -r build/* /var/www/trip/domestic/; else echo 'Build failed, not copying files'; exit 1; fi",
    "build:partner": "rm -rf build && cross-env NODE_OPTIONS=--max_old_space_size=4096 NODE_ENV=production GENERATE_SOURCEMAP=false REACT_APP_TYPE=PARTNER PUBLIC_URL=/ craco build && if [ -d 'build/static' ]; then rm -rf /var/www/partner && mkdir -p /var/www/partner && cp -r build/* /var/www/partner/; else echo 'Build failed, not copying files'; exit 1; fi",
    "build:home": "rm -rf build && cross-env NODE_OPTIONS=--max_old_space_size=4096 NODE_ENV=production GENERATE_SOURCEMAP=false REACT_APP_TYPE=HOME PUBLIC_URL=/ craco build && if [ -d 'build/static' ]; then rm -rf /var/www/home && mkdir -p /var/www/home && cp -r build/* /var/www/home/; else echo 'Build failed, not copying files'; exit 1; fi",
    "build:safety": "rm -rf build && cross-env NODE_OPTIONS=--max_old_space_size=4096 NODE_ENV=production GENERATE_SOURCEMAP=false REACT_APP_TYPE=SAFETY PUBLIC_URL=/safety craco build && if [ -d 'build/static' ]; then rm -rf /var/www/safety && mkdir -p /var/www/safety && cp -r build/* /var/www/safety/; else echo 'Build failed, not copying files'; exit 1; fi",
    "build:disaster": "rm -rf build && cross-env NODE_OPTIONS=--max_old_space_size=4096 NODE_ENV=production GENERATE_SOURCEMAP=false REACT_APP_TYPE=DISASTER REACT_APP_DISASTER_APP=safeguard PUBLIC_URL=/safety/disaster craco build && if [ -d 'build/static' ]; then rm -rf /var/www/safety/disaster && mkdir -p /var/www/safety/disaster && cp -r build/* /var/www/safety/disaster/; else echo 'Build failed, not copying files'; exit 1; fi",
    "build:safeguard": "rm -rf build && cross-env NODE_OPTIONS=--max_old_space_size=4096 NODE_ENV=production GENERATE_SOURCEMAP=false REACT_APP_TYPE=DISASTER REACT_APP_DISASTER_APP=safeguard PUBLIC_URL=/safety/disasterSafeguard craco build && if [ -d 'build/static' ]; then rm -rf /var/www/safety/disasterSafeguard && mkdir -p /var/www/safety/disasterSafeguard && cp -r build/* /var/www/safety/disasterSafeguard/; else echo 'Build failed, not copying files'; exit 1; fi",
    "build:house": "rm -rf build && cross-env NODE_OPTIONS=--max_old_space_size=4096 NODE_ENV=production GENERATE_SOURCEMAP=false REACT_APP_TYPE=DISASTER REACT_APP_DISASTER_APP=house PUBLIC_URL=/safety/disasterHouse craco build && if [ -d 'build/static' ]; then rm -rf /var/www/safety/disasterHouse && mkdir -p /var/www/safety/disasterHouse && cp -r build/* /var/www/safety/disasterHouse/; else echo 'Build failed, not copying files'; exit 1; fi",
    "build:governance": "rm -rf build && cross-env NODE_OPTIONS=--max_old_space_size=4096 NODE_ENV=production GENERATE_SOURCEMAP=false REACT_APP_TYPE=DISASTER REACT_APP_DISASTER_APP=governance PUBLIC_URL=/safety/disasterGovernance craco build && if [ -d 'build/static' ]; then rm -rf /var/www/safety/disasterGovernance && mkdir -p /var/www/safety/disasterGovernance && cp -r build/* /var/www/safety/disasterGovernance/; else echo 'Build failed, not copying files'; exit 1; fi",
    "build:fire": "rm -rf build && cross-env NODE_OPTIONS=--max_old_space_size=4096 NODE_ENV=production GENERATE_SOURCEMAP=false REACT_APP_TYPE=FIRE PUBLIC_URL=/safety/fire craco build && if [ -d 'build/static' ]; then rm -rf /var/www/safety/fire && mkdir -p /var/www/safety/fire && cp -r build/* /var/www/safety/fire/; else echo 'Build failed, not copying files'; exit 1; fi",
    "build:certificate": "rm -rf build && cross-env NODE_OPTIONS=--max_old_space_size=4096 NODE_ENV=production GENERATE_SOURCEMAP=false REACT_APP_TYPE=CERTIFICATE PUBLIC_URL=/certificate craco build && if [ -d 'build/static' ]; then rm -rf /var/www/certificate && mkdir -p /var/www/certificate && cp -r build/* /var/www/certificate/; else echo 'Build failed, not copying files'; exit 1; fi",
    "build:all": "rm -rf /var/www/trip /var/www/trip/overseas /var/www/trip/departed /var/www/trip/longterm /var/www/trip/domestic /var/www/claim /var/www/safety /var/www/safety/disaster /var/www/safety/fire /var/www/safety/disasterSafeguard /var/www/safety/disasterHouse /var/www/safety/disasterGovernance /var/www/home /var/www/partner /var/www/certificate && yarn build:home && yarn build:trip && yarn build:overseas && yarn build:departed && yarn build:longterm && yarn build:domestic && yarn build:claim && yarn build:safety && yarn build:safeguard && yarn build:house && yarn build:governance && yarn build:fire && yarn build:partner && yarn build:certificate"
  },
  "eslintConfig": {
    "extends": [
      "react-app",
      "react-app/jest"
    ]
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  },
  "devDependencies": {
    "@craco/craco": "^7.1.0",
    "cross-env": "^7.0.3"
  }
}
